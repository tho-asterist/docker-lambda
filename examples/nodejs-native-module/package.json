{
  "name": "module-native",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "docker run --rm -v \"$PWD\":/var/task mlupin/docker-lambda:build-nodejs12.x npm rebuild --build-from-source",
    "test": "node test.js"
  },
  "dependencies": {
    "bcrypt": "^5.0.0"
  }
}
